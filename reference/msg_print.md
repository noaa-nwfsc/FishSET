# Display temporary file using message or print

If shiny is running, prints. Otherwise, uses message.

## Usage

``` r
msg_print(temp_file)
```

## Arguments

- temp_file:

  temporary file to display. j
