# Convert vector to scalar

Collapses a vector to a scalar. This is needed to convert an R list to
an html list.

## Usage

``` r
collapse_leaf(x)
```

## Arguments

- x:

  A vector or object in a list.

## Value

a character vector of length 1.
