# Find list depth

Iterates
[`purrr::vec_depth()`](https://purrr.tidyverse.org/reference/pluck_depth.html)
over each object in a list.

## Usage

``` r
list_depth(l)
```

## Arguments

- l:

  A list.

## Value

A numeric vector of list depths.
