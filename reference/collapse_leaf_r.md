# Recursively apply `collapse_leaf()` to a list object

Recursively apply [`collapse_leaf()`](collapse_leaf.md) to a list object

## Usage

``` r
collapse_leaf_r(l)
```

## Arguments

- l:

  A list

## Value

A list.
