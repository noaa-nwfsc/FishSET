# View the most recent fleet table by project

View the most recent fleet table by project

## Usage

``` r
view_fleet_table(project)
```

## Arguments

- project:

  The name of project.

## Examples

``` r
if (FALSE) { # \dontrun{
view_fleet_table("pollock")
} # }
```
