# Retrieve project settings

Retrieve project settings

## Usage

``` r
get_proj_settings(project, format = FALSE)
```

## Arguments

- project:

  Name of project.

- format:

  Logical, output project settings using `pander`. Useful for markdown
  documents.

## Details

The project settings file includes confidentiality settings, the user
output folder directory, and the default plot saving size.
