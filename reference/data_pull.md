# Pull data from sqlite database

Pull data from sqlite database

## Usage

``` r
data_pull(dat, project)
```

## Arguments

- dat:

  Data table name (character) or a data frame object

- project:

  Project name
