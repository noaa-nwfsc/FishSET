# Return unique rows

Check whether all rows are in a dataset are unique. If not, returns only
the unique rows.

## Usage

``` r
unique_rows(dat)
```

## Arguments

- dat:

  Data to check for non-unique rows.

## Examples

``` r
if (FALSE) { # \dontrun{
dat <- unique_rows(dat)
} # }
```
