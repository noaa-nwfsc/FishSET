# Find categorical columns in data frame

Find categorical columns in data frame

## Usage

``` r
category_cols(dat, out = "names")
```

## Arguments

- dat:

  MainDataTable, dataframe, or list to check.

- out:

  Whether to return the column `"names"` (the default) or a logical
  vector (`"logical"`).

## Examples

``` r
if (FALSE) { # \dontrun{
category_cols(pollockMainDataTable)
} # }
```
