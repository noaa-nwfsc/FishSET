# Is list object a leaf node

Determines whether an object in a list is a leaf node.

## Usage

``` r
is_leaf(x)
```

## Arguments

- x:

  An object in a list.

## Value

`TRUE` if list object is a leaf node, `FALSE` if not.
