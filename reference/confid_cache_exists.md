# Confidentialy cache exists

Returns `TRUE` if confidentiality cache file is found in the project
output folder.

## Usage

``` r
confid_cache_exists(project)
```

## Arguments

- project:

  Name of project.

## Examples

``` r
if (FALSE) { # \dontrun{
confid_cache_exists("pollock")
} # }
```
