# Convert DD to DMS

Convert decimal degree format to degree minutes seconds format.

## Usage

``` r
dd2dms(dd, NS = FALSE)
```

## Arguments

- dd:

  numeric vector of decimal degrees.

- NS:

  logical, `TRUE` for north/south decimal degrees, `FALSE` for east/west
  decimal degrees.
