# Find how many standard deviations point `x` is from mean of `y`.

Find how many standard deviations point `x` is from mean of `y`.

## Usage

``` r
find_dev(x, y)
```

## Arguments

- x:

  value to check

- y:

  data vector. Must be numeric
