# Parse date/date-time variable

Parse date/date-time variable

## Usage

``` r
date_check(dat, date)
```

## Arguments

- dat:

  Dataframe containing variable to convert to date/date-time.

- date:

  Date variable name to convert.

## Value

Checks whether variable can be converted to date or date-time. Returns
dataframe with converted variable.
