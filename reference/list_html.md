# Create an HTML list item

Converts a list object to an HTML list item (\<li\>). If an object is a
dataframe or matrix, it is converted to an HTML table first.

## Usage

``` r
list_html(l)
```

## Arguments

- l:

  A list.

## Value

An HTML string of a list item.
