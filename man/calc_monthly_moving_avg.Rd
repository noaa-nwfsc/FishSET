% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_monthly_moving_avg.R
\name{calc_monthly_moving_avg}
\alias{calc_monthly_moving_avg}
\title{Calculate monthly moving window averages}
\usage{
calc_monthly_moving_avg(
  df,
  name,
  year_col,
  month_col,
  group_cols,
  value_col,
  window_size = 3,
  month_lag = 1,
  year_lag = 0,
  fill_empty_expectation = NA
)
}
\arguments{
\item{df}{Primary data frame or data.table containing the observations.}

\item{name}{String, the name of the new column to be added to \code{df} 
containing the calculated moving averages. Defaults to \code{"moving_avg"}.}

\item{year_col}{String, the name of the column containing the year (integer).}

\item{month_col}{String, the name of the column containing the month (integer).}

\item{group_cols}{Character vector, variable(s) from \code{df} that define how to 
group the data for averaging (e.g. \code{c("VESSEL_ID", "ZONE")}). The moving 
average is calculated independently for each group defined here.}

\item{value_col}{String, the name of the column containing the numeric values 
to be averaged.}

\item{window_size}{Numeric, the size of the moving window in months. Defaults to 3.}

\item{month_lag}{Numeric, the number of months to lag the window. Defaults to 1.
For example, a lag of 1 starts the average from the previous month.}

\item{year_lag}{Numeric, the number of years to lag the reference date. Defaults to 0.
If set to 1, the function looks up the date from exactly one year prior before 
applying the \code{month_lag} and \code{window_size}.}

\item{fill_empty_expectation}{Numeric or \code{NA}. Value used to fill \code{NA} 
values in the resulting moving average column. Defaults to \code{NA}.}
}
\value{
Returns the original \code{df} as a data.table with the additional 
  column specified by \code{name}.
}
\description{
Calculate monthly moving window averages
}
