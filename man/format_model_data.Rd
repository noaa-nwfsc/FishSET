% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_model_data.R
\name{format_model_data}
\alias{format_model_data}
\title{Format Model Data}
\usage{
format_model_data(
  project,
  name,
  alt_name,
  zone_id,
  unique_obs_id,
  select_vars = NULL,
  aux_data = NULL,
  aux_key = NULL,
  gridded_data = NULL,
  grid_var_name = NULL,
  grid_time_var = NULL,
  expectations = NULL,
  distance = TRUE,
  distance_units = NULL,
  crs = NULL,
  impute = NULL
)
}
\arguments{
\item{project}{Name of the project.}

\item{name}{Name for this specific formatted model data instance. Must be unique within the 
project's formatted data list.}

\item{alt_name}{Name of the alternative choice matrix.}

\item{zone_id}{Variable name in the dataset representing the zone identifier.}

\item{unique_obs_id}{Variable name in the dataset representing the unique observation
identifier (unique rows in the main data table).}

\item{select_vars}{Character vector of variable names to retain from the main data table.
Although this input is optional, it is recommended to limit the final format to necessary
variables for computational efficiency. IMPORTANT NOTE: if modeling multi-haul data, 
be sure to include the lagged zone ID (previous location) in this vector.}

\item{aux_data}{Name of the auxiliary data table to join. Use \code{\link{list_tables}}
function to view the table name.}

\item{aux_key}{Variable name used to join the main data table with the auxiliary data.}

\item{gridded_data}{Name of the gridded data table to join. Use \code{\link{list_tables}}
function to view the table name.}

\item{grid_var_name}{Name to use for the new variable representing the value in the gridded
data.}

\item{grid_time_var}{Variable name representing the time dimension for joining gridded data.
Only use this input if the gridded data varies by space and time.}

\item{expectations}{Character vector containing the names of expected catch or revenue matrices
to merge into the dataset.}

\item{distance}{Logical. If 'TRUE', calculates and merges a distance matrix between observations
and zones. Defaults to 'TRUE'.}

\item{distance_units}{String representing the units of measurement for distance ("km" or "mi").}

\item{crs}{Coordinate reference system. Only used if 'distance = TRUE' and spatial calculations
are required.}

\item{impute}{Method for imputing missing values (NAs). Options are `"mean"`, 
`"median"`, `"mode"`, or `"remove"`. `"Remove"` will completely remove zones from the dataset
that contain any NAs in corresponding data. If NULL, the function stops if NAs are detected.}
}
\value{
A list containing the formatted data frame and the input settings. The list is saved to
 the project database.
}
\description{
Reshapes the project datasets into a single long format suitable for discrete choice modeling
using RTMB. This function integrates various data sources-including distance matrices,
expectation (catch and/or revenue) matrices, auxiliary data, and gridded environmental data-
and performs optional missing data imputation.
}
\details{
The resulting formatted data is serialized and stored in the FishSET project database within a
table named '[project_name]LongFormatData'.
}
\examples{
\dontrun{
  # Basic usage: Formatting data with simple mean imputation for missing values
  format_model_data(
    project = "NewEnglandCod",
    name = "ModelData_Run1",
    alt_name = "altname_1",
    zone_id = "zone_code",
    unique_obs_id = "trip_id",
    select_vars = c("vessel_length", "month", "permit_type"),
    impute = "mean"
  )

  # Advanced usage: Including distance calculations, auxiliary economic data, 
  # and revenue expectations
  format_model_data(
    project = "WestCoastGroundfish",
    name = "ModelData_Spatial",
    alt_name = "altname_1",
    zone_id = "grid_id",
    unique_obs_id = "haul_id",
    select_vars = c("vessel_len", "month", "fuel_price"),
    aux_data = "FuelCostIndex",
    aux_key = "year",
    expectations = c("ExpectedRevenue_2023"),
    distance = TRUE,
    crs = 4326,
    impute = "remove"
  )
}

}
