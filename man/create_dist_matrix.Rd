% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_dist_matrix.R
\name{create_dist_matrix}
\alias{create_dist_matrix}
\title{Create the distance matrix}
\usage{
create_dist_matrix(
  dataset,
  unique_obs_id,
  zoneID,
  spat = NULL,
  spatID = NULL,
  alt_var,
  occasion,
  occasion_var = NULL,
  dataZoneTrue,
  zone_cent = NULL,
  fish_cent = NULL,
  choice,
  units,
  port = NULL,
  crs = NULL
)
}
\arguments{
\item{dataset}{Primary data set}

\item{unique_obs_id}{Column name in 'dataset' that represents unique observation (row) id.}

\item{zoneID}{Column name in 'dataset' for the zone identifier.}

\item{spat}{Spatial table (sf object) containing polygons or points for locations.}

\item{spatID}{Column name in 'spat' for the Zone/area ID.}

\item{alt_var}{Defines the alternative choice location. One of c("zonal centroid", "fishing
centroid", "nearest point").}

\item{occasion}{Defines the origin location. One of c("zonal centroid", "fishing centroid", 
"port", "lon-lat").}

\item{occasion_var}{Variable(s) needed to define the origin location.
- For 'occasion = "centroid"': The column name for the previous area.
- For 'occasion = "port"': The column name in 'dataset' for port.
- For 'occasion = "lon-lat"': A character vector of c(lon, lat) column names. Note that
longitude must be the first column name in the vector.}

\item{dataZoneTrue}{Logical vector indicating which rows (observations) meet the minimum
haul/obs requirement.}

\item{zone_cent}{Zonal centroid table (data.frame with zone ID, cent.lon, cent.lat).}

\item{fish_cent}{Fishing centroid table (data.frame with zone ID, cent.lon, cent.lat).}

\item{choice}{Vector of observed choice zones, same length as 'dataset'.}

\item{units}{Distance units (e.g., "km", "mi", "nm"). Passed to 'sf::st_distance'.}

\item{port}{Port table (data.frame with Port_Name, Port_Long, Port_Lat).}

\item{crs}{Coordinate reference system (numeric EPSG code or PROJ string).}
}
\value{
A list containing:
\itemize{
  \item `dist_matrix`: The calculated distance matrix.
  \item `alt_choice_units`: The units of distance.
  \item `alt_choice_type`: "distance".
  \item `occasion`: The origin type used.
  \item `occasion_var`: The variable(s) used for the origin.
  \item `alt_choice`: The destination type used.
}
}
\description{
Create the distance matrix
}
\details{
Function is called by \code{\link{make_model_design}} to generate the
  distance matrix. Alternative fishing options come from the Alternative Choice
  list, generated from the \code{\link{create_alternative_choice}} function.
}
\keyword{internal}
