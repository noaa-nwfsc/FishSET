
format_model_data <- function(project,
                              name,
                              select_vars = NULL,
                              aux_data = FALSE,
                              gridded_data = FALSE,
                              expectations = NULL,
                              distance = TRUE,
                              alt_name,
                              impute){
  
  
  
  return(NULL)
}